- name: install and start nginx
  hosts: web
  become: yes
  tasks:
  - name: install nginx
    ansible.builtin.dnf:
     name: nginx
     state: installed
  - name: running nginx
    ansible.builtin.service:
      name: nginx
      state: stopped
      enabled: yes


